{
  "name": "sto-shakedown",
  "version": "0.6.3",
  "private": true,
  "main": "launcher/electron.cjs",
  "type": "module",
  "description": "Local STO build experiments grounded in combat logs",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "electron": "44.3.0"
  }
}
